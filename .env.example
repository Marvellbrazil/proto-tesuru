#define WIFI_SSID "ssid"
#define WIFI_PASS "pass"
#define LAPTOP_IP "192.168.1.1"
#if 0
# Read by the Python client only. The #if 0 guard hides this block from the C++
# preprocessor, which cannot parse shell-style assignments. python-dotenv has no
# notion of #if, so it simply skips these #-comments and reads the line below.
EXCEL_FILENAME="sensordata.csv" # dont change it if you don't know what are you doing
#endif